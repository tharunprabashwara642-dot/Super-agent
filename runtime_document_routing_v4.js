const fs=require('fs');const path=require('path');
(function(){const file=path.join(__dirname,'index.js');let s=fs.readFileSync(file,'utf8');const marker='// DOCUMENT_ROUTING_V4';if(s.includes(marker))return;
const start=s.indexOf('async function callBrain(contents, systemInstruction) {');const end=s.indexOf('\n\nasync function fetchRecentConversation',start);if(start<0||end<0)throw new Error('callBrain boundaries not found');
const replacement=`async function callBrain(contents, systemInstruction) {\n  const userText=(contents||[]).filter(m=>m?.role==='user').flatMap(m=>m?.parts||[]).map(p=>p?.text||'').join(' ');\n  const isDocument=/\\b(pdf|document|report|worksheet|model\\s*paper|question\\s*paper|mcq|notes|handout|certificate)\\b/i.test(userText)||/\\b(පීඩීඑෆ්|වාර්තාව|ප්‍රශ්න පත්‍ර|මොඩල් පේපර්|සටහන්|ලේඛනය)\\b/i.test(userText);\n  const declarations=CHAT_TOOLS[0].functionDeclarations.filter(d=>!isDocument||![\"add_custom_tool\",\"delete_custom_tool\",\"edit_own_code\",\"insert_own_code\",\"update_own_code\",\"run_shell_command\"].includes(d.name));\n  const custom=isDocument?[]:customToolDeclarations;\n  const tools=[{functionDeclarations:[...declarations,...mcpToolDeclarations,...custom]}];\n  const instruction=String(systemInstruction||'')+(isDocument?'\\n\\nHARD DOCUMENT RULE: For a PDF/MCQ/document request you MUST call the native document tool. Never create/install a custom tool for this request. Never claim a file was delivered unless the document tool returns delivery_verified=true with a Telegram file_id. If document generation fails, retry/repair the document path; do not switch to web/code-reading and then falsely report success.':'');\n  return await nvidiaChatShimmed(contents,instruction,tools);\n}`;
s=s.slice(0,start)+replacement+s.slice(end);s+=`\n${marker}\n`;fs.writeFileSync(file,s);})();
